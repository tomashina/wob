{{ header }}
<div class="container">
  <ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
    {% endfor %}
  </ul>
  <div class="row">{{ column_left }}
    {% if column_left and column_right %}
    {% set class = 'col-sm-6' %}
    {% elseif column_left or column_right %}
    {% set class = 'col-md-9 col-sm-8' %}
    {% else %}
    {% set class = 'col-sm-12' %}
    {% endif %}
    <div id="content" class="{{ class }} info information-page-content">
    {{ content_top }}
      <h1 id="page-title">{{ heading_title }}</h1>
      <div class="information-copy">
        {{ description }}
      </div>
      {{ content_bottom }}
     </div>
    {{ column_right }}
  </div>
</div>
{{ footer }}
