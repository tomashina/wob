{{ header }}
<div class="container">
  <ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
    {% endfor %}
  </ul>
  <div class="row">{{ column_left }}
    {% if column_left and column_right %}
    {% set class = 'col-sm-6' %}
    {% elseif column_left or column_right %}
    {% set class = 'col-md-9 col-sm-8' %}
    {% else %}
    {% set class = 'col-sm-12' %}
    {% endif %}
    <div id="content" class="{{ class }}">{{ content_top }}
      <h1 id="page-title">{{ heading_title }}</h1>
      <div class="inline-link text-center" style="border:3px solid #ad974f;
padding:50px ;">{{ text_message }}

 <div class="text-center"><img src="{{ scan }}" style="max-width:500px"/></div>
</div>

   


      {{ content_bottom }}</div>
    {{ column_right }}</div>
</div>
{{ footer }}